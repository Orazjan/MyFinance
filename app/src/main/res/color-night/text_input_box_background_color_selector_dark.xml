<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools" tools:ignore="MissingDefaultResource">
    <!-- Цвет фона при фокусе (используем цвет фона карточек) -->
    <item android:color="@color/card_background_light" android:state_focused="true" />
    <!-- Цвет фона при наведении (используем цвет фона карточек) -->
    <item android:color="@color/card_background_light" android:state_hovered="true" />
    <!-- Цвет фона в выключенном состоянии (используем цвет фона карточек) -->
    <item android:color="@color/card_background_light" android:state_enabled="false" />
    <!-- Цвет фона по умолчанию (используем цвет фона карточек) -->
    <item android:color="@color/card_background_light" />
</selector>
